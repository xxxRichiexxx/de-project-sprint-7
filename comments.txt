Кирилл, добрый день!

    "Не очень понял, запись в одну строчку вполне корректна, а вот float на float64 нам не поможет, ранее писал какой нужен тип"

Насколько я понял, в Python нет отдельного тиа даннных, как double. Вот, что пишут на эту тему:

    Встроенный float тип Python имеет двойную точность (это C doubleв CPython, Java doubleв Jython).
    Если вам нужна большая точность, получите NumPy и используйте его numpy.float128.
    https://stackoverflow.com/questions/6663272/double-precision-floating-values-in-python

Соответственно, я не понял, что нужно поправить в случае чтения csv через pandas.
Единственное, что приходит в голову, это прочитать csv с помощью spark.read.csv(), заменить запятые на точки и преобразовать широту и долготу к DoubleType().
Прошу уточнить, как именно нужно обеспечить более высокую точность?

П.С чтобы меньше переделывать применил вот такой финт:
    from decimal import Decimal

    cities = pd.read_csv(url, sep=sep)
    cities['lat'] = cities['lat'].apply(
        lambda y: y.replace(',', '.')
    ).apply(Decimal)
    cities['lng'] = cities['lng'].apply(
        lambda y: y.replace(',', '.')
    ).apply(Decimal)
количество знаков после запятой увеличил.

    Каталог tmp отлетел со всем содержимым, правильно понял, что не понадобится?)

Данный каталог - временный. 
Там были наработки по реализации витрин в несколько шагов. Я решил отказаться от этой идеи, т.к не успеваю(
Поэтому удалил данный каталог.